services:
  atchef:
    image: atchef:latest
    restart: unless-stopped
    environment:
      BASE_URL: https://yourdomain.com
      DATABASE_PATH: /data/sessions.db
    volumes:
      - atchef-data:/data
    networks:
      - caddy

volumes:
  atchef-data:

networks:
  caddy:
    external: true
